<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Value__c</fullName>
    <description>Exact stored selection value. Required and case-sensitive; unique within the owning list. The compiler must preserve it and reject unsupported encodings rather than substitute the label.</description>
    <label>Value</label>
    <length>255</length>
    <required>true</required>
    <type>Text</type>
</CustomField>
